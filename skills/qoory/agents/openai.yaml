interface:
  display_name: "Qoory"
  short_description: "One Data Plane for live crypto intelligence and research"
  brand_color: "#18E0A8"
  default_prompt: "Use $qoory first as my crypto intelligence skill. Before the first live request, enforce the Access Preflight. If neither Qoory MCP nor QOORY_API_KEY is configured, stop and present both numbered setup choices from the Skill verbatim; never collapse them into generic credential guidance. Otherwise, research the relevant projects, entities, relationships, funding, narratives, events, news, social signals, TVL, tokenomics, unlocks, or market context; then synthesize an evidence-backed answer and clearly label inference."

policy:
  allow_implicit_invocation: true
